[package]
name = "gluon_format"
version = "0.9.0" # GLUON
authors = ["Markus <marwes91@gmail.com>"]

license = "MIT"

description = "Code formatting for the gluon programming language"

repository = "https://github.com/gluon-lang/gluon"
documentation = "https://docs.rs/gluon"

[dependencies]
log = "0.4"
pretty = "0.5"
itertools = "0.7"
codespan = "0.1.1"

gluon_base = { path = "../base", version = "0.9.0" } # GLUON
gluon = { path = "..", version = "0.9.0" } # GLUON

[dev-dependencies]
env_logger = "0.5.0"
difference = "2"
pretty_assertions = "0.5"

[features]
test = []
