/// Modes for opening a file. Used with `std.io.open_file` and `std.io.create_file`
type OpenOptions =
    | Read
    | Write
    | Append
    | Truncate
    | Create
    | CreateNew

{ OpenOptions }
