//@NO-IMPLICIT-PRELUDE

let { Monad } = import! std.prelude

#[implicit]
type Transformer t = {
    monad : forall m . [Monad m] -> Monad (t m),
    wrap_monad : forall a m . [Monad m] -> m a -> t m a
}

{ Transformer }
